#!/usr/bin/python
# coding: utf8

"""
ETL2OSM API
===========

import etl2osm

# Step by step
data = etl2osm.load(infile)
data.transform(config)
data.save(outfile)

# All in one
etl2osm.process(infile, outfile config)
"""